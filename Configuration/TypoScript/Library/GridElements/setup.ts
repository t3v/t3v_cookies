lib {
  plugin {
    tx_t3vcookies {
      gridElements {
        noteGridElement {
          container = TEXT
          container {
            value = {$plugin.tx_t3vcookies.gridElements.noteGridElement.container}
          }

          closeControl = TEXT
          closeControl {
            value = {$plugin.tx_t3vcookies.gridElements.noteGridElement.closeControl}
          }
        }
      }
    }
  }
}
