lib {
  plugin {
    tx_t3vcookies {
      defaults {
        gridElements {
          noteGridElement {
            container = TEXT
            container {
              value = {$plugin.tx_t3vcookies.defaults.gridElements.noteGridElement.container}
            }

            closeControl = TEXT
            closeControl {
              value = {$plugin.tx_t3vcookies.defaults.gridElements.noteGridElement.closeControl}
            }
          }
        }
      }
    }
  }
}